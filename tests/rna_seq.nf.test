nextflow_pipeline {
 
 
 name "Test pipeline with rnaseq database generation"
    script "../main.nf"
    tag "pipeline"

    test("params: --skip_rnaseqdb false") {

        when {
            params {

                skip_rnaseqdb = false

                //RNASEQDB inputs
                bam_file  = "$projectDir/assets/test_files/input_1.sorted.bam"
                bam_index  = "$projectDir/assets/test_files/input_1.sorted.bam.bai"
                annotation  = "$projectDir/assets/test_files/Saccharomyces_cerevisiae.R64-1-1.115.gtf"
                reference   = "$projectDir/assets/test_files/Saccharomyces_cerevisiae.R64-1-1.dna.toplevel.fa"
                transcripts = "$projectDir/assets/test_files/Saccharomyces_cerevisiae.R64-1-1.cdna.all.fa"
                
                outdir        = "${outputDir}" 
            
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // stable_path: All files in ${params.outdir}/ with stable content
            def stable_path = getAllFilesFromDir(params.outdir, ignoreFile: 'tests/.nftignore')
            assertAll(
                { assert workflow.success},
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
                    removeNextflowVersion("$outputDir/pipeline_info/nf_core_proteogenomicsdb_software_mqc_versions.yml"),
                    // All stable path name, with a relative path
                    stable_name,
                    // All files with stable contents
                    stable_path
                ).match() }
            )
        }
    }

    test("params: --skip_rnaseqdb false -stub") {

        options "-stub"

        when {
            params {

                skip_rnaseqdb = false

                //RNASEQDB inputs
                bam_file  = "$projectDir/assets/test_files/input_1.sorted.bam"
                bam_index  = "$projectDir/assets/test_files/input_1.sorted.bam.bai"
                annotation  = "$projectDir/assets/test_files/Saccharomyces_cerevisiae.R64-1-1.115.gtf"
                reference   = "$projectDir/assets/test_files/Saccharomyces_cerevisiae.R64-1-1.dna.toplevel.fa"
                transcripts = "$projectDir/assets/test_files/Saccharomyces_cerevisiae.R64-1-1.cdna.all.fa"
                
                outdir        = "${outputDir}" 
            
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // stable_path: All files in ${params.outdir}/ with stable content
            def stable_path = getAllFilesFromDir(params.outdir, ignoreFile: 'tests/.nftignore')
            assertAll(
                { assert workflow.success},
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
                    removeNextflowVersion("$outputDir/pipeline_info/nf_core_proteogenomicsdb_software_mqc_versions.yml"),
                    // All stable path name, with a relative path
                    stable_name,
                    // All files with stable contents
                    stable_path
                ).match() }
            )
        }
    }
}